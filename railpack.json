{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "20",
    "python": "3.13"
  },
  "build": {
    "commands": [
      "cd /app/theme/static_src && npm install --include=dev",
      "cd /app && python manage.py tailwind build",
      "cd /app && python manage.py collectstatic --noinput --clear",
      "cd /app && ls -la theme/static/css/dist/ || echo '=== dist folder missing ==='",
      "cd /app && find theme -name styles.css || echo '=== styles.css not found ==='"
    ]
  }
}