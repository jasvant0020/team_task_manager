{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "20",
    "python": "3.13"
  },
  "build": {
    "commands": [
      "cd theme/static_src && npm ci --include=dev",
      "python manage.py tailwind build",
      "python manage.py collectstatic --noinput --clear"
    ]
  }
}